# zk scaffolding

- `circuits/` – place STARK/zkVM circuits, test vectors.
- `prover/` – prover integration hooks.
- `verifier/` – verifier bindings for on-chain/off-chain checks.

Current status: placeholders only; wire to VM and contracts per implementation order.

